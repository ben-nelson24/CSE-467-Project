# Set up Crawler

## Pre-Reqs
 - You will need to download and install android studio to run the emulator https://developer.android.com/studio
 - Droidbot is set up as a submodule for this project

## Install Dependencies for Droidbot
- in /crawler/droidbot run: ```pip install -e .```

